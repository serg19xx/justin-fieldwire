# Быстрый старт: Шаблоны задач с мок данными

## Что было сделано

✅ Создана система шаблонов задач  
✅ Импортированы все 206 задач из `Task List.tsv` в мок данные  
✅ Автоматическая загрузка мок данных при первом использовании  
✅ Готово к миграции в базу данных  

## Использование

### 1. Открыть шаблоны задач

1. Откройте любой проект
2. Перейдите в раздел **Tasks**
3. Нажмите кнопку **"📋 From Templates"**

### 2. Выбрать задачи

- Все 206 задач уже доступны в списке
- Используйте поиск для быстрого поиска
- Фильтруйте по категориям (Planning, HVAC, Plumbing, и т.д.)
- Выберите нужные задачи (можно выбрать все)

### 3. Создать задачи

1. Нажмите **"Continue"**
2. Установите дату начала проекта
3. При необходимости назначьте дефолтного бригадира
4. Настройте параметры для отдельных задач
5. Нажмите **"Create X Tasks"**

## Мок данные

### Расположение

- **JSON файл**: `src/core/mock-data/task-templates.json`
- **Загрузчик**: `src/core/mock-data/task-templates-loader.ts`
- **Источник**: `migrations/Task List.tsv`

### Автоматическая загрузка

Мок данные автоматически загружаются когда:
- Нет шаблонов в localStorage
- API endpoint недоступен
- Пользователь открывает шаблоны впервые

### Статистика

- **Всего шаблонов**: 206
- **Категорий**: 20+
- **Milestones**: ~50+

## Миграция в базу данных

### Шаг 1: Создать таблицу

```sql
CREATE TABLE task_templates (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  category VARCHAR(100),
  duration_days INTEGER,
  start_offset_days INTEGER,
  milestone VARCHAR(50),
  status VARCHAR(50) DEFAULT 'planned',
  task_order INTEGER,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### Шаг 2: Импортировать данные

#### Вариант A: SQL скрипт

Создайте SQL скрипт из JSON файла:

```sql
INSERT INTO task_templates (name, category, duration_days, start_offset_days, milestone, status, task_order)
VALUES
  ('Client Business Planning Meeting 1', 'Planning & Design', 3, 0, 'meeting', 'planned', 1),
  -- ... (все 206 шаблонов)
;
```

#### Вариант B: Backend API

Создайте endpoint для импорта:

```typescript
POST /api/v1/task-templates/import
Body: { templates: TaskTemplate[] }
```

И вызовите из фронтенда:

```typescript
import { getMockTaskTemplates } from '@/core/mock-data/task-templates-loader'

const templates = getMockTaskTemplates()
await api.post('/api/v1/task-templates/import', { templates })
```

### Шаг 3: Обновить API

После импорта в БД, API автоматически будет использовать данные из базы вместо мок данных.

## Преимущества

✅ **Готово к использованию** - все 206 задач доступны сразу  
✅ **Быстро** - не нужно ждать создания БД  
✅ **Гибко** - можно выбрать любые задачи  
✅ **Удобно** - автоматическая категоризация и определение типов  
✅ **Масштабируемо** - легко мигрировать в БД позже  

## Дополнительная информация

- [Полная документация по шаблонам](./task-templates.md)
- [Документация по мок данным](./task-templates-mock-data.md)
- [Руководство по импорту](./task-templates-import-guide.md)


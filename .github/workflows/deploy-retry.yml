name: Deploy to WHC.ca (with retry)

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Type check
        run: npm run type-check

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Copy .htaccess
        run: cp .htaccess dist/

      - name: Deploy to WHC.ca (Attempt 1)
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        continue-on-error: true
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ./public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.env*
          dangerous-clean-slate: false
          log-level: verbose
          timeout: 60000

      - name: Wait before retry
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        run: sleep 30

      - name: Deploy to WHC.ca (Attempt 2)
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        continue-on-error: true
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ./public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.env*
          dangerous-clean-slate: false
          log-level: verbose
          timeout: 60000

      - name: Deploy to WHC.ca (Final Attempt)
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ./public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.env*
          dangerous-clean-slate: false
          log-level: verbose
          timeout: 60000
